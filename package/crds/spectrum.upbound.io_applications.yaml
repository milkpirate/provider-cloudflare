---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: applications.spectrum.upbound.io
spec:
  group: spectrum.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the Applications API. Provides
          a Cloudflare Spectrum Application. You can extend the power of Cloudflare's
          DDoS, TLS, and IP Firewall to your other TCP-based services.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  argoSmartRouting:
                    description: (Boolean) Enables Argo Smart Routing. Enables Argo
                      Smart Routing.
                    type: boolean
                  dns:
                    description: '(Block List, Min: 1, Max: 1) The name and type of
                      DNS record for the Spectrum application. (see below for nested
                      schema) The name and type of DNS record for the Spectrum application.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. The name of the DNS record associated
                            with the application.
                          type: string
                        type:
                          description: (String) The type of DNS record associated
                            with the application. The type of DNS record associated
                            with the application.
                          type: string
                      type: object
                    type: array
                  edgeIps:
                    description: '(Block List, Max: 1) The anycast edge IP configuration
                      for the hostname of this application. (see below for nested
                      schema) The anycast edge IP configuration for the hostname of
                      this application.'
                    items:
                      properties:
                        connectivity:
                          description: '(String) The IP versions supported for inbound
                            connections on Spectrum anycast IPs. Required when type
                            is not static. Available values: all, ipv4, ipv6. The
                            IP versions supported for inbound connections on Spectrum
                            anycast IPs. Required when `type` is not `static`. Available
                            values: `all`, `ipv4`, `ipv6`.'
                          type: string
                        ips:
                          description: (Set of String) The collection of customer
                            owned IPs to broadcast via anycast for this hostname and
                            application. Requires Bring Your Own IP provisioned. The
                            collection of customer owned IPs to broadcast via anycast
                            for this hostname and application. Requires [Bring Your
                            Own IP](https://developers.cloudflare.com/spectrum/getting-started/byoip/)
                            provisioned.
                          items:
                            type: string
                          type: array
                        type:
                          description: '(String) The type of DNS record associated
                            with the application. The type of edge IP configuration
                            specified. Available values: `dynamic`, `static`.'
                          type: string
                      type: object
                    type: array
                  ipFirewall:
                    description: (Boolean) Enables the IP Firewall for this application.
                      Enables the IP Firewall for this application.
                    type: boolean
                  originDirect:
                    description: (List of String) A list of destination addresses
                      to the origin. e.g. tcp://192.0.2.1:22. A list of destination
                      addresses to the origin. e.g. `tcp://192.0.2.1:22`.
                    items:
                      type: string
                    type: array
                  originDns:
                    description: '(Block List, Max: 1) A destination DNS addresses
                      to the origin. (see below for nested schema) A destination DNS
                      addresses to the origin.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. Fully qualified domain name of the
                            origin.
                          type: string
                      type: object
                    type: array
                  originPort:
                    description: (Number) Origin port to proxy traffice to. Conflicts
                      with origin_port_range. Origin port to proxy traffice to. Conflicts
                      with `origin_port_range`.
                    type: number
                  originPortRange:
                    description: 23. Conflicts with origin_port. (see below for nested
                      schema) Origin port range to proxy traffice to. When using a
                      range, the protocol field must also specify a range, e.g. `tcp/22-23`.
                      Conflicts with `origin_port`.
                    items:
                      properties:
                        end:
                          description: (Number) Upper bound of the origin port range.
                            Upper bound of the origin port range.
                          type: number
                        start:
                          description: (Number) Lower bound of the origin port range.
                            Lower bound of the origin port range.
                          type: number
                      type: object
                    type: array
                  protocol:
                    description: (String) The port configuration at Cloudflare's edge.
                      e.g. tcp/22. The port configuration at Cloudflare's edge. e.g.
                      `tcp/22`.
                    type: string
                  proxyProtocol:
                    description: '(String) Enables a proxy protocol to the origin.
                      Available values: off, v1, v2, simple. Enables a proxy protocol
                      to the origin. Available values: `off`, `v1`, `v2`, `simple`.'
                    type: string
                  tls:
                    description: '(String) TLS configuration option for Cloudflare
                      to connect to your origin. Available values: off, flexible,
                      full, strict. TLS configuration option for Cloudflare to connect
                      to your origin. Available values: `off`, `flexible`, `full`,
                      `strict`.'
                    type: string
                  trafficType:
                    description: '(String) Sets application type. Available values:
                      direct, http, https. Sets application type. Available values:
                      `direct`, `http`, `https`.'
                    type: string
                  zoneId:
                    description: (String) The zone identifier to target for the resource.
                      The zone identifier to target for the resource.
                    type: string
                  zoneIdRef:
                    description: Reference to a Zone in zone to populate zoneId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  zoneIdSelector:
                    description: Selector for a Zone in zone to populate zoneId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  argoSmartRouting:
                    description: (Boolean) Enables Argo Smart Routing. Enables Argo
                      Smart Routing.
                    type: boolean
                  dns:
                    description: '(Block List, Min: 1, Max: 1) The name and type of
                      DNS record for the Spectrum application. (see below for nested
                      schema) The name and type of DNS record for the Spectrum application.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. The name of the DNS record associated
                            with the application.
                          type: string
                        type:
                          description: (String) The type of DNS record associated
                            with the application. The type of DNS record associated
                            with the application.
                          type: string
                      type: object
                    type: array
                  edgeIps:
                    description: '(Block List, Max: 1) The anycast edge IP configuration
                      for the hostname of this application. (see below for nested
                      schema) The anycast edge IP configuration for the hostname of
                      this application.'
                    items:
                      properties:
                        connectivity:
                          description: '(String) The IP versions supported for inbound
                            connections on Spectrum anycast IPs. Required when type
                            is not static. Available values: all, ipv4, ipv6. The
                            IP versions supported for inbound connections on Spectrum
                            anycast IPs. Required when `type` is not `static`. Available
                            values: `all`, `ipv4`, `ipv6`.'
                          type: string
                        ips:
                          description: (Set of String) The collection of customer
                            owned IPs to broadcast via anycast for this hostname and
                            application. Requires Bring Your Own IP provisioned. The
                            collection of customer owned IPs to broadcast via anycast
                            for this hostname and application. Requires [Bring Your
                            Own IP](https://developers.cloudflare.com/spectrum/getting-started/byoip/)
                            provisioned.
                          items:
                            type: string
                          type: array
                        type:
                          description: '(String) The type of DNS record associated
                            with the application. The type of edge IP configuration
                            specified. Available values: `dynamic`, `static`.'
                          type: string
                      type: object
                    type: array
                  ipFirewall:
                    description: (Boolean) Enables the IP Firewall for this application.
                      Enables the IP Firewall for this application.
                    type: boolean
                  originDirect:
                    description: (List of String) A list of destination addresses
                      to the origin. e.g. tcp://192.0.2.1:22. A list of destination
                      addresses to the origin. e.g. `tcp://192.0.2.1:22`.
                    items:
                      type: string
                    type: array
                  originDns:
                    description: '(Block List, Max: 1) A destination DNS addresses
                      to the origin. (see below for nested schema) A destination DNS
                      addresses to the origin.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. Fully qualified domain name of the
                            origin.
                          type: string
                      type: object
                    type: array
                  originPort:
                    description: (Number) Origin port to proxy traffice to. Conflicts
                      with origin_port_range. Origin port to proxy traffice to. Conflicts
                      with `origin_port_range`.
                    type: number
                  originPortRange:
                    description: 23. Conflicts with origin_port. (see below for nested
                      schema) Origin port range to proxy traffice to. When using a
                      range, the protocol field must also specify a range, e.g. `tcp/22-23`.
                      Conflicts with `origin_port`.
                    items:
                      properties:
                        end:
                          description: (Number) Upper bound of the origin port range.
                            Upper bound of the origin port range.
                          type: number
                        start:
                          description: (Number) Lower bound of the origin port range.
                            Lower bound of the origin port range.
                          type: number
                      type: object
                    type: array
                  protocol:
                    description: (String) The port configuration at Cloudflare's edge.
                      e.g. tcp/22. The port configuration at Cloudflare's edge. e.g.
                      `tcp/22`.
                    type: string
                  proxyProtocol:
                    description: '(String) Enables a proxy protocol to the origin.
                      Available values: off, v1, v2, simple. Enables a proxy protocol
                      to the origin. Available values: `off`, `v1`, `v2`, `simple`.'
                    type: string
                  tls:
                    description: '(String) TLS configuration option for Cloudflare
                      to connect to your origin. Available values: off, flexible,
                      full, strict. TLS configuration option for Cloudflare to connect
                      to your origin. Available values: `off`, `flexible`, `full`,
                      `strict`.'
                    type: string
                  trafficType:
                    description: '(String) Sets application type. Available values:
                      direct, http, https. Sets application type. Available values:
                      `direct`, `http`, `https`.'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dns is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dns)
                || (has(self.initProvider) && has(self.initProvider.dns))'
            - message: spec.forProvider.protocol is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocol)
                || (has(self.initProvider) && has(self.initProvider.protocol))'
          status:
            description: ApplicationStatus defines the observed state of Application.
            properties:
              atProvider:
                properties:
                  argoSmartRouting:
                    description: (Boolean) Enables Argo Smart Routing. Enables Argo
                      Smart Routing.
                    type: boolean
                  dns:
                    description: '(Block List, Min: 1, Max: 1) The name and type of
                      DNS record for the Spectrum application. (see below for nested
                      schema) The name and type of DNS record for the Spectrum application.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. The name of the DNS record associated
                            with the application.
                          type: string
                        type:
                          description: (String) The type of DNS record associated
                            with the application. The type of DNS record associated
                            with the application.
                          type: string
                      type: object
                    type: array
                  edgeIps:
                    description: '(Block List, Max: 1) The anycast edge IP configuration
                      for the hostname of this application. (see below for nested
                      schema) The anycast edge IP configuration for the hostname of
                      this application.'
                    items:
                      properties:
                        connectivity:
                          description: '(String) The IP versions supported for inbound
                            connections on Spectrum anycast IPs. Required when type
                            is not static. Available values: all, ipv4, ipv6. The
                            IP versions supported for inbound connections on Spectrum
                            anycast IPs. Required when `type` is not `static`. Available
                            values: `all`, `ipv4`, `ipv6`.'
                          type: string
                        ips:
                          description: (Set of String) The collection of customer
                            owned IPs to broadcast via anycast for this hostname and
                            application. Requires Bring Your Own IP provisioned. The
                            collection of customer owned IPs to broadcast via anycast
                            for this hostname and application. Requires [Bring Your
                            Own IP](https://developers.cloudflare.com/spectrum/getting-started/byoip/)
                            provisioned.
                          items:
                            type: string
                          type: array
                        type:
                          description: '(String) The type of DNS record associated
                            with the application. The type of edge IP configuration
                            specified. Available values: `dynamic`, `static`.'
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ipFirewall:
                    description: (Boolean) Enables the IP Firewall for this application.
                      Enables the IP Firewall for this application.
                    type: boolean
                  originDirect:
                    description: (List of String) A list of destination addresses
                      to the origin. e.g. tcp://192.0.2.1:22. A list of destination
                      addresses to the origin. e.g. `tcp://192.0.2.1:22`.
                    items:
                      type: string
                    type: array
                  originDns:
                    description: '(Block List, Max: 1) A destination DNS addresses
                      to the origin. (see below for nested schema) A destination DNS
                      addresses to the origin.'
                    items:
                      properties:
                        name:
                          description: (String) The name of the DNS record associated
                            with the application. Fully qualified domain name of the
                            origin.
                          type: string
                      type: object
                    type: array
                  originPort:
                    description: (Number) Origin port to proxy traffice to. Conflicts
                      with origin_port_range. Origin port to proxy traffice to. Conflicts
                      with `origin_port_range`.
                    type: number
                  originPortRange:
                    description: 23. Conflicts with origin_port. (see below for nested
                      schema) Origin port range to proxy traffice to. When using a
                      range, the protocol field must also specify a range, e.g. `tcp/22-23`.
                      Conflicts with `origin_port`.
                    items:
                      properties:
                        end:
                          description: (Number) Upper bound of the origin port range.
                            Upper bound of the origin port range.
                          type: number
                        start:
                          description: (Number) Lower bound of the origin port range.
                            Lower bound of the origin port range.
                          type: number
                      type: object
                    type: array
                  protocol:
                    description: (String) The port configuration at Cloudflare's edge.
                      e.g. tcp/22. The port configuration at Cloudflare's edge. e.g.
                      `tcp/22`.
                    type: string
                  proxyProtocol:
                    description: '(String) Enables a proxy protocol to the origin.
                      Available values: off, v1, v2, simple. Enables a proxy protocol
                      to the origin. Available values: `off`, `v1`, `v2`, `simple`.'
                    type: string
                  tls:
                    description: '(String) TLS configuration option for Cloudflare
                      to connect to your origin. Available values: off, flexible,
                      full, strict. TLS configuration option for Cloudflare to connect
                      to your origin. Available values: `off`, `flexible`, `full`,
                      `strict`.'
                    type: string
                  trafficType:
                    description: '(String) Sets application type. Available values:
                      direct, http, https. Sets application type. Available values:
                      `direct`, `http`, `https`.'
                    type: string
                  zoneId:
                    description: (String) The zone identifier to target for the resource.
                      The zone identifier to target for the resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
