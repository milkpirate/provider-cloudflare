---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: deviceposturerules.warp.upbound.io
spec:
  group: warp.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: DevicePostureRule
    listKind: DevicePostureRuleList
    plural: deviceposturerules
    singular: deviceposturerule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevicePostureRule is the Schema for the DevicePostureRules API.
          Provides a Cloudflare Device Posture Rule resource. Device posture rules
          configure security policies for device posture checks.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DevicePostureRuleSpec defines the desired state of DevicePostureRule
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: (String) The account identifier to target for the
                      resource. The account identifier to target for the resource.
                    type: string
                  accountIdRef:
                    description: Reference to a Account in account to populate accountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accountIdSelector:
                    description: Selector for a Account in account to populate accountId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  description:
                    description: (String)
                    type: string
                  expiration:
                    description: (String) Expire posture results after the specified
                      amount of time. Must be in the format 1h or 30m. Valid units
                      are h and m. Expire posture results after the specified amount
                      of time. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  input:
                    description: (Block List) Required for all rule types except warp,
                      gateway, and tanium. (see below for nested schema) Required
                      for all rule types except `warp`, `gateway`, and `tanium`.
                    items:
                      properties:
                        activeThreats:
                          description: (Number) The number of active threats from
                            SentinelOne. The number of active threats from SentinelOne.
                          type: number
                        certificateId:
                          description: (String) The UUID of a Cloudflare managed certificate.
                            The UUID of a Cloudflare managed certificate.
                          type: string
                        checkDisks:
                          description: (Set of String) Specific volume(s) to check
                            for encryption. Specific volume(s) to check for encryption.
                          items:
                            type: string
                          type: array
                        cn:
                          description: (String) The common name for a certificate.
                            The common name for a certificate.
                          type: string
                        complianceStatus:
                          description: '(String) The workspace one device compliance
                            status. Available values: compliant, noncompliant. The
                            workspace one device compliance status. Available values:
                            `compliant`, `noncompliant`.'
                          type: string
                        connectionId:
                          description: (String) The workspace one connection id. The
                            workspace one connection id.
                          type: string
                        countOperator:
                          description: '(String) The count comparison operator for
                            kolide. Available values: >, >=, <, <=, ==. The count
                            comparison operator for kolide. Available values: `>`,
                            `>=`, `<`, `<=`, `==`.'
                          type: string
                        domain:
                          description: (String) The domain that the client must join.
                            The domain that the client must join.
                          type: string
                        eidLastSeen:
                          description: (String) The datetime a device last seen in
                            RFC 3339 format from Tanium. The datetime a device last
                            seen in RFC 3339 format from Tanium.
                          type: string
                        enabled:
                          description: (Boolean) True if the firewall must be enabled.
                            True if the firewall must be enabled.
                          type: boolean
                        exists:
                          description: (Boolean) Checks if the file should exist.
                            Checks if the file should exist.
                          type: boolean
                        id:
                          description: (String) The ID of this resource. The Teams
                            List id. Required for `serial_number` and `unique_client_id`
                            rule types.
                          type: string
                        infected:
                          description: (Boolean) True if SentinelOne device is infected.
                            True if SentinelOne device is infected.
                          type: boolean
                        isActive:
                          description: (Boolean) True if SentinelOne device is active.
                            True if SentinelOne device is active.
                          type: boolean
                        issueCount:
                          description: (String) The number of issues for kolide. The
                            number of issues for kolide.
                          type: string
                        lastSeen:
                          description: (String) The duration of time that the host
                            was last seen from Crowdstrike. Must be in the format
                            1h or 30m. Valid units are d, h and m. The duration of
                            time that the host was last seen from Crowdstrike. Must
                            be in the format `1h` or `30m`. Valid units are `d`, `h`
                            and `m`.
                          type: string
                        networkStatus:
                          description: '(String) The network status from SentinelOne.
                            Available values: connected, disconnected, disconnecting,
                            connecting. The network status from SentinelOne. Available
                            values: `connected`, `disconnected`, `disconnecting`,
                            `connecting`.'
                          type: string
                        operator:
                          description: '(String) The version comparison operator.
                            Available values: >, >=, <, <=, ==. The version comparison
                            operator. Available values: `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                        os:
                          description: (String) OS signal score from Crowdstrike.
                            Value must be between 1 and 100. OS signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        osDistroName:
                          description: (String) The operating system excluding version
                            information. The operating system excluding version information.
                          type: string
                        osDistroRevision:
                          description: (String) The operating system version excluding
                            OS name information or release name. The operating system
                            version excluding OS name information or release name.
                          type: string
                        overall:
                          description: (String) Overall ZTA score from Crowdstrike.
                            Value must be between 1 and 100. Overall ZTA score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        path:
                          description: (String) The path to the file. The path to
                            the file.
                          type: string
                        requireAll:
                          description: (Boolean) True if all drives must be encrypted.
                            True if all drives must be encrypted.
                          type: boolean
                        riskLevel:
                          description: '(String) The risk level from Tanium. Available
                            values: low, medium, high, critical. The risk level from
                            Tanium. Available values: `low`, `medium`, `high`, `critical`.'
                          type: string
                        running:
                          description: (Boolean) Checks if the application should
                            be running. Checks if the application should be running.
                          type: boolean
                        sensorConfig:
                          description: (String) Sensor signal score from Crowdstrike.
                            Value must be between 1 and 100. Sensor signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        sha256:
                          description: (String) The sha256 hash of the file. The sha256
                            hash of the file.
                          type: string
                        state:
                          description: '(String) The host’s current online status
                            from Crowdstrike. Available values: online, offline, unknown.
                            The host’s current online status from Crowdstrike. Available
                            values: `online`, `offline`, `unknown`.'
                          type: string
                        thumbprint:
                          description: (String) The thumbprint of the file certificate.
                            The thumbprint of the file certificate.
                          type: string
                        totalScore:
                          description: (Number) The total score from Tanium. The total
                            score from Tanium.
                          type: number
                        version:
                          description: (String) The operating system semantic version.
                            The operating system semantic version.
                          type: string
                        versionOperator:
                          description: '(String) The version comparison operator for
                            crowdstrike. Available values: >, >=, <, <=, ==. The version
                            comparison operator for crowdstrike. Available values:
                            `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                      type: object
                    type: array
                  match:
                    description: (Block List) The conditions that the client must
                      match to run the rule. (see below for nested schema) The conditions
                      that the client must match to run the rule.
                    items:
                      properties:
                        platform:
                          description: '(String) The platform of the device. Available
                            values: windows, mac, linux, android, ios, chromeos. The
                            platform of the device. Available values: `windows`, `mac`,
                            `linux`, `android`, `ios`, `chromeos`.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: (String) Name of the device posture rule. Name of
                      the device posture rule.
                    type: string
                  schedule:
                    description: (String) Tells the client when to run the device
                      posture check. Must be in the format 1h or 30m. Valid units
                      are h and m. Tells the client when to run the device posture
                      check. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  type:
                    description: '(String) The device posture rule type. Available
                      values: serial_number, file, application, gateway, warp, domain_joined,
                      os_version, disk_encryption, firewall, client_certificate, workspace_one,
                      unique_client_id, crowdstrike_s2s, sentinelone, kolide, tanium_s2s,
                      intune, sentinelone_s2s. The device posture rule type. Available
                      values: `serial_number`, `file`, `application`, `gateway`, `warp`,
                      `domain_joined`, `os_version`, `disk_encryption`, `firewall`,
                      `client_certificate`, `workspace_one`, `unique_client_id`, `crowdstrike_s2s`,
                      `sentinelone`, `kolide`, `tanium_s2s`, `intune`, `sentinelone_s2s`.'
                    type: string
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  description:
                    description: (String)
                    type: string
                  expiration:
                    description: (String) Expire posture results after the specified
                      amount of time. Must be in the format 1h or 30m. Valid units
                      are h and m. Expire posture results after the specified amount
                      of time. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  input:
                    description: (Block List) Required for all rule types except warp,
                      gateway, and tanium. (see below for nested schema) Required
                      for all rule types except `warp`, `gateway`, and `tanium`.
                    items:
                      properties:
                        activeThreats:
                          description: (Number) The number of active threats from
                            SentinelOne. The number of active threats from SentinelOne.
                          type: number
                        certificateId:
                          description: (String) The UUID of a Cloudflare managed certificate.
                            The UUID of a Cloudflare managed certificate.
                          type: string
                        checkDisks:
                          description: (Set of String) Specific volume(s) to check
                            for encryption. Specific volume(s) to check for encryption.
                          items:
                            type: string
                          type: array
                        cn:
                          description: (String) The common name for a certificate.
                            The common name for a certificate.
                          type: string
                        complianceStatus:
                          description: '(String) The workspace one device compliance
                            status. Available values: compliant, noncompliant. The
                            workspace one device compliance status. Available values:
                            `compliant`, `noncompliant`.'
                          type: string
                        connectionId:
                          description: (String) The workspace one connection id. The
                            workspace one connection id.
                          type: string
                        countOperator:
                          description: '(String) The count comparison operator for
                            kolide. Available values: >, >=, <, <=, ==. The count
                            comparison operator for kolide. Available values: `>`,
                            `>=`, `<`, `<=`, `==`.'
                          type: string
                        domain:
                          description: (String) The domain that the client must join.
                            The domain that the client must join.
                          type: string
                        eidLastSeen:
                          description: (String) The datetime a device last seen in
                            RFC 3339 format from Tanium. The datetime a device last
                            seen in RFC 3339 format from Tanium.
                          type: string
                        enabled:
                          description: (Boolean) True if the firewall must be enabled.
                            True if the firewall must be enabled.
                          type: boolean
                        exists:
                          description: (Boolean) Checks if the file should exist.
                            Checks if the file should exist.
                          type: boolean
                        id:
                          description: (String) The ID of this resource. The Teams
                            List id. Required for `serial_number` and `unique_client_id`
                            rule types.
                          type: string
                        infected:
                          description: (Boolean) True if SentinelOne device is infected.
                            True if SentinelOne device is infected.
                          type: boolean
                        isActive:
                          description: (Boolean) True if SentinelOne device is active.
                            True if SentinelOne device is active.
                          type: boolean
                        issueCount:
                          description: (String) The number of issues for kolide. The
                            number of issues for kolide.
                          type: string
                        lastSeen:
                          description: (String) The duration of time that the host
                            was last seen from Crowdstrike. Must be in the format
                            1h or 30m. Valid units are d, h and m. The duration of
                            time that the host was last seen from Crowdstrike. Must
                            be in the format `1h` or `30m`. Valid units are `d`, `h`
                            and `m`.
                          type: string
                        networkStatus:
                          description: '(String) The network status from SentinelOne.
                            Available values: connected, disconnected, disconnecting,
                            connecting. The network status from SentinelOne. Available
                            values: `connected`, `disconnected`, `disconnecting`,
                            `connecting`.'
                          type: string
                        operator:
                          description: '(String) The version comparison operator.
                            Available values: >, >=, <, <=, ==. The version comparison
                            operator. Available values: `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                        os:
                          description: (String) OS signal score from Crowdstrike.
                            Value must be between 1 and 100. OS signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        osDistroName:
                          description: (String) The operating system excluding version
                            information. The operating system excluding version information.
                          type: string
                        osDistroRevision:
                          description: (String) The operating system version excluding
                            OS name information or release name. The operating system
                            version excluding OS name information or release name.
                          type: string
                        overall:
                          description: (String) Overall ZTA score from Crowdstrike.
                            Value must be between 1 and 100. Overall ZTA score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        path:
                          description: (String) The path to the file. The path to
                            the file.
                          type: string
                        requireAll:
                          description: (Boolean) True if all drives must be encrypted.
                            True if all drives must be encrypted.
                          type: boolean
                        riskLevel:
                          description: '(String) The risk level from Tanium. Available
                            values: low, medium, high, critical. The risk level from
                            Tanium. Available values: `low`, `medium`, `high`, `critical`.'
                          type: string
                        running:
                          description: (Boolean) Checks if the application should
                            be running. Checks if the application should be running.
                          type: boolean
                        sensorConfig:
                          description: (String) Sensor signal score from Crowdstrike.
                            Value must be between 1 and 100. Sensor signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        sha256:
                          description: (String) The sha256 hash of the file. The sha256
                            hash of the file.
                          type: string
                        state:
                          description: '(String) The host’s current online status
                            from Crowdstrike. Available values: online, offline, unknown.
                            The host’s current online status from Crowdstrike. Available
                            values: `online`, `offline`, `unknown`.'
                          type: string
                        thumbprint:
                          description: (String) The thumbprint of the file certificate.
                            The thumbprint of the file certificate.
                          type: string
                        totalScore:
                          description: (Number) The total score from Tanium. The total
                            score from Tanium.
                          type: number
                        version:
                          description: (String) The operating system semantic version.
                            The operating system semantic version.
                          type: string
                        versionOperator:
                          description: '(String) The version comparison operator for
                            crowdstrike. Available values: >, >=, <, <=, ==. The version
                            comparison operator for crowdstrike. Available values:
                            `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                      type: object
                    type: array
                  match:
                    description: (Block List) The conditions that the client must
                      match to run the rule. (see below for nested schema) The conditions
                      that the client must match to run the rule.
                    items:
                      properties:
                        platform:
                          description: '(String) The platform of the device. Available
                            values: windows, mac, linux, android, ios, chromeos. The
                            platform of the device. Available values: `windows`, `mac`,
                            `linux`, `android`, `ios`, `chromeos`.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: (String) Name of the device posture rule. Name of
                      the device posture rule.
                    type: string
                  schedule:
                    description: (String) Tells the client when to run the device
                      posture check. Must be in the format 1h or 30m. Valid units
                      are h and m. Tells the client when to run the device posture
                      check. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  type:
                    description: '(String) The device posture rule type. Available
                      values: serial_number, file, application, gateway, warp, domain_joined,
                      os_version, disk_encryption, firewall, client_certificate, workspace_one,
                      unique_client_id, crowdstrike_s2s, sentinelone, kolide, tanium_s2s,
                      intune, sentinelone_s2s. The device posture rule type. Available
                      values: `serial_number`, `file`, `application`, `gateway`, `warp`,
                      `domain_joined`, `os_version`, `disk_encryption`, `firewall`,
                      `client_certificate`, `workspace_one`, `unique_client_id`, `crowdstrike_s2s`,
                      `sentinelone`, `kolide`, `tanium_s2s`, `intune`, `sentinelone_s2s`.'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: DevicePostureRuleStatus defines the observed state of DevicePostureRule.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: (String) The account identifier to target for the
                      resource. The account identifier to target for the resource.
                    type: string
                  description:
                    description: (String)
                    type: string
                  expiration:
                    description: (String) Expire posture results after the specified
                      amount of time. Must be in the format 1h or 30m. Valid units
                      are h and m. Expire posture results after the specified amount
                      of time. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  input:
                    description: (Block List) Required for all rule types except warp,
                      gateway, and tanium. (see below for nested schema) Required
                      for all rule types except `warp`, `gateway`, and `tanium`.
                    items:
                      properties:
                        activeThreats:
                          description: (Number) The number of active threats from
                            SentinelOne. The number of active threats from SentinelOne.
                          type: number
                        certificateId:
                          description: (String) The UUID of a Cloudflare managed certificate.
                            The UUID of a Cloudflare managed certificate.
                          type: string
                        checkDisks:
                          description: (Set of String) Specific volume(s) to check
                            for encryption. Specific volume(s) to check for encryption.
                          items:
                            type: string
                          type: array
                        cn:
                          description: (String) The common name for a certificate.
                            The common name for a certificate.
                          type: string
                        complianceStatus:
                          description: '(String) The workspace one device compliance
                            status. Available values: compliant, noncompliant. The
                            workspace one device compliance status. Available values:
                            `compliant`, `noncompliant`.'
                          type: string
                        connectionId:
                          description: (String) The workspace one connection id. The
                            workspace one connection id.
                          type: string
                        countOperator:
                          description: '(String) The count comparison operator for
                            kolide. Available values: >, >=, <, <=, ==. The count
                            comparison operator for kolide. Available values: `>`,
                            `>=`, `<`, `<=`, `==`.'
                          type: string
                        domain:
                          description: (String) The domain that the client must join.
                            The domain that the client must join.
                          type: string
                        eidLastSeen:
                          description: (String) The datetime a device last seen in
                            RFC 3339 format from Tanium. The datetime a device last
                            seen in RFC 3339 format from Tanium.
                          type: string
                        enabled:
                          description: (Boolean) True if the firewall must be enabled.
                            True if the firewall must be enabled.
                          type: boolean
                        exists:
                          description: (Boolean) Checks if the file should exist.
                            Checks if the file should exist.
                          type: boolean
                        id:
                          description: (String) The ID of this resource. The Teams
                            List id. Required for `serial_number` and `unique_client_id`
                            rule types.
                          type: string
                        infected:
                          description: (Boolean) True if SentinelOne device is infected.
                            True if SentinelOne device is infected.
                          type: boolean
                        isActive:
                          description: (Boolean) True if SentinelOne device is active.
                            True if SentinelOne device is active.
                          type: boolean
                        issueCount:
                          description: (String) The number of issues for kolide. The
                            number of issues for kolide.
                          type: string
                        lastSeen:
                          description: (String) The duration of time that the host
                            was last seen from Crowdstrike. Must be in the format
                            1h or 30m. Valid units are d, h and m. The duration of
                            time that the host was last seen from Crowdstrike. Must
                            be in the format `1h` or `30m`. Valid units are `d`, `h`
                            and `m`.
                          type: string
                        networkStatus:
                          description: '(String) The network status from SentinelOne.
                            Available values: connected, disconnected, disconnecting,
                            connecting. The network status from SentinelOne. Available
                            values: `connected`, `disconnected`, `disconnecting`,
                            `connecting`.'
                          type: string
                        operator:
                          description: '(String) The version comparison operator.
                            Available values: >, >=, <, <=, ==. The version comparison
                            operator. Available values: `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                        os:
                          description: (String) OS signal score from Crowdstrike.
                            Value must be between 1 and 100. OS signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        osDistroName:
                          description: (String) The operating system excluding version
                            information. The operating system excluding version information.
                          type: string
                        osDistroRevision:
                          description: (String) The operating system version excluding
                            OS name information or release name. The operating system
                            version excluding OS name information or release name.
                          type: string
                        overall:
                          description: (String) Overall ZTA score from Crowdstrike.
                            Value must be between 1 and 100. Overall ZTA score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        path:
                          description: (String) The path to the file. The path to
                            the file.
                          type: string
                        requireAll:
                          description: (Boolean) True if all drives must be encrypted.
                            True if all drives must be encrypted.
                          type: boolean
                        riskLevel:
                          description: '(String) The risk level from Tanium. Available
                            values: low, medium, high, critical. The risk level from
                            Tanium. Available values: `low`, `medium`, `high`, `critical`.'
                          type: string
                        running:
                          description: (Boolean) Checks if the application should
                            be running. Checks if the application should be running.
                          type: boolean
                        sensorConfig:
                          description: (String) Sensor signal score from Crowdstrike.
                            Value must be between 1 and 100. Sensor signal score from
                            Crowdstrike. Value must be between 1 and 100.
                          type: string
                        sha256:
                          description: (String) The sha256 hash of the file. The sha256
                            hash of the file.
                          type: string
                        state:
                          description: '(String) The host’s current online status
                            from Crowdstrike. Available values: online, offline, unknown.
                            The host’s current online status from Crowdstrike. Available
                            values: `online`, `offline`, `unknown`.'
                          type: string
                        thumbprint:
                          description: (String) The thumbprint of the file certificate.
                            The thumbprint of the file certificate.
                          type: string
                        totalScore:
                          description: (Number) The total score from Tanium. The total
                            score from Tanium.
                          type: number
                        version:
                          description: (String) The operating system semantic version.
                            The operating system semantic version.
                          type: string
                        versionOperator:
                          description: '(String) The version comparison operator for
                            crowdstrike. Available values: >, >=, <, <=, ==. The version
                            comparison operator for crowdstrike. Available values:
                            `>`, `>=`, `<`, `<=`, `==`.'
                          type: string
                      type: object
                    type: array
                  match:
                    description: (Block List) The conditions that the client must
                      match to run the rule. (see below for nested schema) The conditions
                      that the client must match to run the rule.
                    items:
                      properties:
                        platform:
                          description: '(String) The platform of the device. Available
                            values: windows, mac, linux, android, ios, chromeos. The
                            platform of the device. Available values: `windows`, `mac`,
                            `linux`, `android`, `ios`, `chromeos`.'
                          type: string
                      type: object
                    type: array
                  name:
                    description: (String) Name of the device posture rule. Name of
                      the device posture rule.
                    type: string
                  schedule:
                    description: (String) Tells the client when to run the device
                      posture check. Must be in the format 1h or 30m. Valid units
                      are h and m. Tells the client when to run the device posture
                      check. Must be in the format `1h` or `30m`. Valid units are
                      `h` and `m`.
                    type: string
                  type:
                    description: '(String) The device posture rule type. Available
                      values: serial_number, file, application, gateway, warp, domain_joined,
                      os_version, disk_encryption, firewall, client_certificate, workspace_one,
                      unique_client_id, crowdstrike_s2s, sentinelone, kolide, tanium_s2s,
                      intune, sentinelone_s2s. The device posture rule type. Available
                      values: `serial_number`, `file`, `application`, `gateway`, `warp`,
                      `domain_joined`, `os_version`, `disk_encryption`, `firewall`,
                      `client_certificate`, `workspace_one`, `unique_client_id`, `crowdstrike_s2s`,
                      `sentinelone`, `kolide`, `tanium_s2s`, `intune`, `sentinelone_s2s`.'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
